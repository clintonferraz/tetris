export enum Direction {
    Up,
    Right,
    Down,
    Left
}

export enum PieceType {
    L,
    T,
    I,
    J,
    O
}

export enum SquareType {
    Empty = 'type0',
    CurrentPiece = 'type1',
    StackedPiece = 'type2',
    Type3 = 'type3'
}